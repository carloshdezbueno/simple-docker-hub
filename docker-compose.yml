services:
  hub:
    build: .
    container_name: rpi-hub
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./data:/data
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped
